<?xml version="1.0" encoding="UTF-8" ?>
<Module>
  <ModulePrefs title="Notifiy" height="30">
    <Require feature="wave" />
    <Require feature="locked-domain" />
    <Require feature="dynamic-height"/>
  </ModulePrefs>
  <Content type="html">
    <![CDATA[
        <style type="text/css"> 
            .border {
                font-size: small;
                padding: 5px;
                border: 1px solid #EEE;
                border-top-color: #CCC;
                background: #EEE;
                border-radius: 6px;
                -moz-border-radius: 6px;
                -webkit-border-radius: 6px;
            }

            .border img {
                vertical-align: text-bottom;
                cursor: pointer;
                width: 15px;
                height: 15px;
            }

            #title {
                cursor: pointer;
            }

            .more {
                float: right;
                color: #666;
                font-size: x-small;
                margin-right: 5px;
            }

            #preferences span {
                font-size: x-large;
            }
        </style>
        <div class="border">
            <a href="#" onclick="toggle_preferences(this); return false;" class="more">More</a>
            <img id="notify" onclick="toggle()" alt="star" title="Toggle notifications">
            <span id="title" onclick="toggle()"></span>
            <div id="preferences" style="display: none;">
                <span>&#8632;</span> Click this star to enable/disable notifications.
            </div>
        </div>
        <script type="text/javascript">

            var URL = "http://wave-email-notifications.appspot.com/";
            var notify = document.getElementById("notify");
            var title = document.getElementById("title");
            var preferences = document.getElementById("preferences");

            function urlencode(string) {
                return escape(string).replace(new RegExp( "\\+", "g"), "%2B")
            }

            function getURL(extra) {
                var state = wave.getState();
                var rand = Math.random();
                var participant = urlencode(wave.getViewer().getId());
                var waveId = urlencode(state.get("waveId"));
                var url =  URL + "proc?participant=" + participant + "&waveId=" + waveId + "&" + rand + extra;

                var params = {};  
                params[gadgets.io.RequestParameters.CONTENT_TYPE] = gadgets.io.ContentType.TEXT;  
                gadgets.io.makeRequest(url, function(obj) {
                    if (obj.text == "on") {
                        title.innerHTML = "Notifications enabled";
                        notify.src = URL + "star_on.gif";
                    } else {
                        title.innerHTML = "Notifications disabled";
                        notify.src = URL + "star_off.gif";
                    }
                    gadgets.window.adjustHeight();
                }, params);
            }

            function toggle() {
                getURL("&toggle=true");
            }

            function toggle_preferences(element) {
                if (preferences.style.display == 'none') {
                    preferences.style.display = 'block';
                    element.innerHTML = "Less";
                } else {
                    preferences.style.display = 'none';
                    element.innerHTML = "More";
                }
                gadgets.window.adjustHeight();
            }

            function stateUpdated() {
                getURL("");
                gadgets.window.adjustHeight();
            }

            function init() {
                if (wave && wave.isInWaveContainer()) {
                    wave.setStateCallback(stateUpdated)
                }
            }

            gadgets.util.registerOnLoadHandler(init);

        </script>
    ]]>
  </Content>
</Module>
